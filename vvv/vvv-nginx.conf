server {
	listen 80;
	server_name minnie.dev;
	location / {
		proxy_set_header    X-Real-IP    $remote_addr;
		proxy_set_header    Host         $http_host;
		proxy_pass          http://192.168.50.1:9090;
	}
}
